ToDo:
Banner text: GRAPHMON JOHNSQL

Store Graphs in SQL as:
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
DB = Graph
Tables:
	=VERT=					=EDGE=
	-ID		<int, unique primary>  	-SOURCE <Foreign Node ID>
	-COLOR 		<int>                 	-TARGER  <Foreign Node ID>
	-DEGREE 	<int>                   -WEIGHT  <int>

	=CLIQUE=
	 -ID      	<int, unique primary>
	 -AMMO	    	<int>
	 -MEMBERS 	<json>

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Metrics W/SQL Queries:
    -Min Degree			get_lowest_degree()
    -Max Degree			get_highest_degree()
    -Average Degree		get_avg_degree()
    -Min Weight			get_lowest_weight()
    -Max Weight			get_highest_weight()
    -Average Weight		get_avg_weight()
    -Largest Clique		get_largest_clique()
    -Number of Cliques		get_clique_amt()

SETTERS:
	insert_vert(INT, INT)	
	insert_edge(PRIMARY KEY INT, PRIMARY KEY INT)
	insert_clique(INT, LONGTEXT JSON)

GETTERS:
	get_clique_by_id(INT)
	get_vert_by_id(INT)

OTHER:
	reset_db()

Dataflow:
    - Graph is generated:
        ->FindCliques
        ->Delete DB, create new DB
        ->Store graph data and cliques in DB
        ->Saved graph to file
        ->Get metrics from SQL
        ->display graph and metrics

    - Extract Cliques
        ->Get Cliques from SQL
        ->ExtractCliques, updates file with new coloring
